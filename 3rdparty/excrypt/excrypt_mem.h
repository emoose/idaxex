#pragma once

int32_t ExCryptMemDiff(uint8_t* buf1, uint8_t* buf2, uint32_t size);

// (not from XeCrypt)
// Reverses the bytes in the data buffer
void ExCryptMemReverseBytes(uint8_t* data, uint32_t length);
